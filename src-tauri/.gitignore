# Cargo
/target/

# Tauri-generated platform projects (regenerated by `tauri android init` /
# `tauri ios init`). Customizations are documented in docs/android-build.md
# and applied as a post-init patch.
/gen/
